.event-category
  .heading
    //- a.label(href="#categories/"+ model.id)
    .line
    .label(data-hook='category-name')= model && model.name
    .line
      //- small= 'View all '+ model.img_count +' images'
  a.category-slideshow!= lz('Slideshow')
  .photos
  .load-more
    .label!= lz('Show More Photos')
    .loader
  //- .heading.siblings
    a.label(data-hook='prev-category')
      span
      small!= lz('Previous Category')
    .line
    a.label(data-hook='next-category')
      span
      small!= lz('Next Category')
