.grid-photo
  .image
    //- img(src= model.thumb_url)
  //- .image(style="background-image:url("+ model.thumb_url +")")
  .fill.borders
  .overlay
    a.view-large(href= model.app_url)
      span #{language.elm['View Larger Image']}
    .icons
      .icon.cart: .tip #{language.elm['Buy']}
      //- .icon.cart: .tip #{language.elm['Buy More']}
      .icon.fav: .tip #{language.elm['To Favorites']}
      //- .icon.fav: .tip #{language.elm['Unfavorite']}
      if model.download
        a.icon.download(href= model.url, target="_blank", bypass): .tip Download
      //- .icon.hide: .tip #{language.elm['Hide Image']}
      .icon.share: .tip  #{language.elm['Share']}
  //- .image
  
