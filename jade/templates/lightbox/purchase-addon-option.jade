.purchase-addon-option(class= model.img_url ? 'has-image' : '')
  .text
    .title
      .checkbox
      = model.name +' (+'+ model.formatMoney(model.price) +')'
    .description= model.description
  if model.img_url
    .image-container(style="background-image:url("+model.img_url+")")
      //- img(src=model.img_url, style="width:"+ model.img_width +'px; height:'+ model.img_height +'px')
      //- img(src=model.img_url)
  
