/*==========================================================================*\
  A single event on the homepage
\*==========================================================================*/
"use strict";

var Parent = require('app/views/base-view');

module.exports = Parent.extend({
  template: require('app/templates').events['event-item'],
  
  bindings: {
    // 'model.locked': {
    //   type: function (el, val) {
    //     this.el.classList.toggle('locked', val);
    //     el.style.backgroundImage = 'url('+
    //         (val ? this.model.thumb_blur_url : this.model.thumb_url) +')';
    //   },
    //   selector: '.bg',
    // },
  },
});
