/*==========================================================================*\
  Fixed header navbar
\*==========================================================================*/
"use strict";

var app = require('app');
var Parent = require('app/views/base-view');

module.exports = Parent.extend({
  dictionary: 'nav',
  autoRender: true,
  template: require('app/templates').global['nav-contents'],
  
  initialize: function () {
    Parent.prototype.initialize.apply(this, arguments);
  },
});

