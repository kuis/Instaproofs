/*==========================================================================*\
  Fixed header navbar
\*==========================================================================*/
"use strict";

var BaseView = require('app/base/base-view');

module.exports = BaseView.extend({
  dictionary: 'nav',
  autoRender: true,
  template: require('app/templates').nav['nav-contents'],
  
  initialize: function () {
    BaseView.prototype.initialize.apply(this, arguments);
  },
});

