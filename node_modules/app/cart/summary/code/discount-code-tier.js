"use strict";

var BaseView = require('app/base/base-view');

module.exports = BaseView.extend({
  viewType: 'discount-code-tier',
  dictionary: 'cartDiscountCode',
  
  template: require('app/templates').cart.summary.code['discount-code-tier'],
  // template: '<div class="discount-code-tier"></div>',
  
  props: {
    
  },
  
  bindings: {
    'model.active': {
      type: 'booleanClass',
      name: 'active',
    },
  },
  
  initialize: function () {
    BaseView.prototype.initialize.apply(this, arguments);
    
    this.listenTo(this.model.code, 'change:type', this.render);
  },
  
  
});
