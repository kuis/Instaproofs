"use strict";

var BaseModel = require('app/base/base-model');

// var debounce = require('lodash.debounce');
// var onChange = debounce(function () {
//   this.changed = true;
// }, 1);

module.exports = BaseModel.extend({
  props: {
    top: 'number',
    left: 'number',
    right: 'number',
    bottom: 'number',
    changed: ['boolean', true, false],
  },
  
  session: {
  },
  
  derived: {
  },
  
  // initialize: function () {
  //   this.listenTo(this, 'change', onChange);
  // },
  
  // serialize: function () {
  //   if (!this.changed) return {};
  //   else return BaseModel.prototype.serialize.apply(this, arguments);
  // },
});
