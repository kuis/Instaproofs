/*==========================================================================*\
  A single photo purchase option's addon in the cart 
\*==========================================================================*/
"use strict";

var BaseView = require('app/base/base-view');

module.exports = BaseView.extend({
  viewType: 'cart-photo-addon',
  template: require('app/templates').cart.photo.addon['cart-photo-addon'],
  
  props: {
  },
  
  bindings: {
  },
  
  initialize: function () {
    BaseView.prototype.initialize.apply(this, arguments);
  },
  
  events: {
  },
  
  // render: function () {
  //   this.renderWithTemplate(this);
  // }
  
  updatePrices: function () {
    
  },
});
