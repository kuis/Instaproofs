- var app = require('app');
- var event = model && model.event;
if event && event.allow_pickup
  option(value="0")!= event.pickup_text
each shipping in app.shipping.models
  option(value=shipping.id)= shipping.name
