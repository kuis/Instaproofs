/*==========================================================================*\
  An overlay view covering pages when user if offline
\*==========================================================================*/
"use strict";

var app = require('app');
var BaseView = require('app/base/base-view');

module.exports = BaseView.extend({
  viewType: 'offline-overlay',
  dictionary: 'event', // localization dictionary 
  
  template: require('app/templates').offline['offline-overlay'],
  
  props: {
    eventId: ['number', true, 0],
  },
  
  events: {
  },
  
  bindings: {
  },
  
  initialize: function (ops) {
    BaseView.prototype.initialize.apply(this, arguments);
    this.render();
  },
  
});



