"use strict";

var BaseModel = require('app/base/base-model');

module.exports = BaseModel.extend({
  // extraProperties: 'ignore',
  props: {
    id: 'number',
    eid: 'number',
    url: 'string', // internal URL 
    type: 'string', // location type e.g. 'event-category' or 'lightbox'
    shortTitle: 'string',
    // page: 'state', // page view instance 
    config: 'object', // configuration
  },
  
  derived: {
  },
});
