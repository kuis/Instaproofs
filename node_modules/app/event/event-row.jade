a.event-row(href= "#events/"+ model.id)
  .td-title= model.name
  .td-date!= model.date ? lz('LL', model.date) : ''
  .td-location!= model.location
  .td-expires!= model.exp_date ? lz('LL', model.exp_date) : ''
