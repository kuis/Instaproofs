a.event-row(href= "#events/"+ model.id)
  .td-title= model.name
  .td-date!= model.date ? lz('LL', model.date) : ''
  .td-type
  if model.date
  .td-
  .info
    h3.event-title= model.name
    h4!= lz('LL', model.date)
    h5= model.type
