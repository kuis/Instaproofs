- var config = require('app').config.custom_crop;
.crop-icon
  .crop-tip
    if config === 'view'
      != lz('Click to view crop')
    else
      != lz('Click to adjust crop')
  .crop-covers
    .crop-cover.top
    .crop-cover.right
    .crop-cover.bottom
    .crop-cover.left
  .borders
