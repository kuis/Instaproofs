/*==========================================================================*\
  List of side-wide notices
\*==========================================================================*/
"use strict";

var Collection = require('app/models/base-collection');

var Notice = require('./base-model').extend({
  props: {
    id: 'number',
    text: 'string',
  },
});

module.exports = Collection.extend({
  model: Notice,
});
