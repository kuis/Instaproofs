"use strict";
var language = require('app/language');

module.exports = require('ampersand-rest-collection').extend({
  model: require('app/models/photo'),
  page: 0,
  perPage: 15,
  complete: false,
  language: language,
  
  // initialize: function (ops) {
  //   ops = ops || {};
  // },
  
  loadMore: function () {
    this.page++;
    this.fetch({ add: true, remove: false, reset: false });
  },
  
  // parse: function (res) {
  //   this.setComplete(!!res.complete);
  //   return res.items;
  // },
});
