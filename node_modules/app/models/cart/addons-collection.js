/*==========================================================================*\
  Collection for purchase addons
\*==========================================================================*/
"use strict";

module.exports = require('app/models/base-collection').extend({
  model: require('./addon-model'),
});
