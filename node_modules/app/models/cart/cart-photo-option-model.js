/*==========================================================================*\
  A photo's purchase option or size in the cart
\*==========================================================================*/
"use strict";

var app = require('app');
var Parent = require('app/models/base-model');

module.exports = Parent.extend({
  props: {
    addons: {
      type: 'object',
      default: function () {
        return {};
      },
    },
  },
  
  collections: {
  },
  
  derived: {
    
  },
  
  // parse: function (res) {
  // },
  
});
