/*==========================================================================*\
  Collection for pricing templates
\*==========================================================================*/
"use strict";

module.exports = require('app/models/base-collection').extend({
  mainIndex: 'pricing_id',
  model: require('./pricing-model'),
});
