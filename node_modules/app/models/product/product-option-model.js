/*==========================================================================*\
  A Purchase Addon Optionm, e.g. paper type
\*==========================================================================*/
"use strict";

module.exports = require('app/models/base-model').extend({
  props: {
    id: 'number',
    name: ['string', true, ''], // name to display
    description: ['string', true, ''], // an optional description
    image: ['string', true, ''], // URL to an option's image
    note: ['string', true, ''], // note on the image
    limited_quantity: ['boolean', true, false], // limited quantity in album
    original_price: ['number', true, 0], // price of an option in cents
    current_price: ['number', true, 0], // price of an option in cents
  },
});
