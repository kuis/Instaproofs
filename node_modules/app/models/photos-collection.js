"use strict";

module.exports = require('app/models/base-collection').extend({
  model: require('./photo-model'),
  page: 0,
  perPage: 15,
  complete: false,
  
  // initialize: function (ops) {
  //   ops = ops || {};
  // },
  
  loadMore: function () {
    this.page++;
    this.fetch({ add: true, remove: false, reset: false });
  },
  
  // parse: function (res) {
  //   this.setComplete(!!res.complete);
  //   return res.items;
  // },
});
