/*============================================================================*\
  List of all specs to be loaded into the karma test runner
\*============================================================================*/
"use strict";

var bodyContents = require('app/templates')['body-contents']();
document.body.innerHTML += bodyContents;

// app module is our main entry point, so we require it first
// to avoid issues with cyclic dependencies 

describe("Base", function () {
  require('app/base/base-model.spec');
  require('app/base/base-collection.spec');
  require('app/base/base-view.spec');
});
describe("Utility", function () {
  require('app/util/helpers/helpers.spec');
  require('app/util/schedule/schedule.spec');
});

