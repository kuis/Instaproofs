.misc-popup.email-request
  .bg
  .box
    .scroller
      .misc-content
        h1!= lz('Email Required')
        p!= lz('To property track your list of favorites, we need an email address to link it to:')
        form.contact-form
          input.email(type="email", name="email")&attributes(lzA('placeholder', 'Your email address...'))
        
    .foot
      .button.cancel.large!= lz('Cancel')
      .button.save.large.highlighted.with-loader
        != lz('Send')
        .loader.small.white
      .success-message!= lz('Email Sent')
