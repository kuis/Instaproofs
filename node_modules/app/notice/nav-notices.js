/*==========================================================================*\
  Dropdown popup with side-wide notices 
\*==========================================================================*/
"use strict";

var BaseView = require('app/base/base-view');
var NoticeView = require('./notice');

var app = require('app');

module.exports = BaseView.extend({
  autoRender: true,
  template: require('app/templates').notice['nav-notices'],
  
  events: {
    'click .button.hide': 'hide',
  },
  
  render: function () {
    this.renderWithTemplate(this);
    this.renderCollection(app.notices, NoticeView, '.items');
  },
  
  hide: function () {
    app.view.hideNotices();
  },
  
});

