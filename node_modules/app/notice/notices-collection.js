/*==========================================================================*\
  List of side-wide notices
\*==========================================================================*/
"use strict";

var BaseCollection = require('app/base/base-collection');

module.exports = BaseCollection.extend({
  model: require('./notice-model'),
});
